## Unescaping HTML Entities with xmlstarlet

The `unesc` command in `xmlstarlet` is designed to unescape XML/HTML entities, making it a handy tool for this specific purpose.
```bash
xmlstarlet unesc input_file.html
```
